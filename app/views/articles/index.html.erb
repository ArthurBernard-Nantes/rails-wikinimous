<h1>Wikinimous</h1>

<div class='container'>
  <% @articles.each do |article| %>
    <div id='article'class='my-3 p-3 table-primary'><%= link_to article.title, article_path(article)%></div>
  <% end %>
</div>

<%= link_to "Create a article", new_article_path%>
